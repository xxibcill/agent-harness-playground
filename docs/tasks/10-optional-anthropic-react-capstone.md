# 10 Optional Anthropic React Capstone

Status: Done

## Goal

Optionally add a final capstone workflow that combines provider reasoning with tool use after the
deterministic ladder is complete.

## Scope

- Evaluate whether the repository needs a workflow such as `anthropic.react`.
- If implemented, keep it clearly marked as advanced and optional.
- Reuse the established workflow concepts instead of inventing a new architecture just for the
  capstone.

## Deliverables

- A decision note or implementation for the capstone workflow.
- Documentation that explains why it belongs at the end of the ladder.
- Tests only if the workflow is actually added.

## Exit Criteria

- The repository can stop at `anthropic.respond` and still feel complete for teaching purposes.
- If the capstone exists, it is clearly separated from the core learning path.
- The added complexity is justified and documented.

## Notes

- This task is optional by design.
- Do not start this task until the deterministic workflow ladder is complete.
